import 'package:flutter_test/flutter_test.dart';

void main() {
  test('adds one to input values', () {
    // final calculator = Calculator();
    /*final view = EditorJSView()
    
    expect(view.addOne(2), 3);
    expect(view.addOne(-7), -6);
    expect(view.addOne(0), 1);
    expect(() => view.addOne(null), throwsNoSuchMethodError);*/
  });
}
